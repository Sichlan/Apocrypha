<UserControl x:Class="Apocrypha.WPF.Controls.CharacterButtonBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Apocrypha.WPF.Controls"
             xmlns:viewModels="clr-namespace:Apocrypha.WPF.ViewModels"
             xmlns:res="clr-namespace:Apocrypha.WPF.Resources.Localization"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel, IsDesignTimeCreatable=False}"
             Visibility="{Binding HasActiveCharacter, Converter={StaticResource BoolToVisibilityConverter}}">
    <Grid Background="{StaticResource BrushPrimary2}" HorizontalAlignment="Stretch">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Command="{Binding SaveCharacterCommand}" Content="{x:Static res:Localization.ButtonLabelSaveCharacter}"/>
        </StackPanel>
    </Grid>
</UserControl>
