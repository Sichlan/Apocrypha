<UserControl x:Class="Apocrypha.WPF.Views.CharacterSelectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewmodels="clr-namespace:Apocrypha.WPF.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodels:CharacterSelectionViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="Aqua" >
        <DataGrid ItemsSource="{Binding Characters}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Lol" Binding="{Binding Budget}"/>
                <DataGridTextColumn Header="Lol" Binding="{Binding Id}"/>
                <DataGridComboBoxColumn Header="lol" ></DataGridComboBoxColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>